{% extends "base.html.j2" %}
{% import 'bootstrap/wtf.html' as wtf %}
{# {% block app_content %}
    <h1>Member Login</h1>
    <div class="row">
        <div class="col-md-4">{{ wtf.quick_form(form) }}</div>
    </div>
    <p>
        New User? <a href="{{ url_for('register') }}">Click to Register</a>
    </p>
    <p>
        Forgot Your Password?
        <a href="{{ url_for('reset_password_request') }}">Click to Reset It</a>
    </p>
{% endblock %} #}
{% block app_content %}
{#<style>
    .container-fluid {
        list-style: none;
        padding: 0;
        margin: 0;
        display: inline-flex; /* 讓選單橫向排列並居中 */
        align-items: center;
    }

    .container-fluidli {
        position: relative;
        display: flex;
        align-items: center;
    }

    .container-fluid li a {
        color: #333;
        text-decoration: none;
        font-size: 14px;
        padding: 0 25px; /* 控制文字間距 */
        transition: color 0.3s;
    }

    .container-fluid li a:hover {
        color: #ed4e51;
    }

    /* 關鍵：紅色垂直分隔線 */
    .container-fluid li:not(:last-child)::after {
        content: "";
        height: 12px; /* 短線高度 */
        width: 1px;
        background-color: #ed4e51; /* 百老匯紅 */
        display: inline-block;
    }
    
    /* 底部最後一條長紅線 */
    .bottom-red-line {
        height: 1px;
        background-color: #ed4e51;
        width: 100%;
        margin-top: 15px;
    }
</style>#}

<nav class="navbar navbar-default" style="border: none; margin-bottom: 0;">
        <div class="container-fluid"> <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav navbar-center" style="display: flex; justify-content: center; float: none;">
                    <li><a href="/ticketing">Ticketing</a></li>
                    <li><a href="/upcoming">Upcoming</a></li>
                    <li><a href="/events">Special</a></li>
                    <li><a href="/cinema">Cinema</a></li>
                    <li><a href="/gift_card">Gift Card Shop</a></li>
                </ul>
            </div>
        </div>
        <div style="height: 2px; background-color: red; width: 100%;"></div>
</nav>
<style>
    body { background-color: #fff; }
    .container { width: 100% !important; max-width: 100% !important; padding: 0 !important; }
    
    .cinema-image {
        width: 100%;
        height: 100vh;
        object-fit: cover;
        display: block;
    }

    .login-form-container {
        padding: 50px;
        display: flex;
        flex-direction: column;
        justify-content: center;
        height: 100vh;
    }

    .form-title {
        font-size: 20px;
        color: #333;
        margin-bottom: 20px;
        position: relative;
        padding-left: 15px;
    }
    .form-title::before {
        content: '';
        position: absolute;
        left: 0;
        top: 3px;
        height: 18px;
        width: 3px;
        background-color: red;
    }

    .form-control-custom {
        border-radius: 0; 
        border: 1px solid #ccc;
        height: 40px;
        margin-bottom: 15px;
        font-size: 14px;
    }

    .btn-custom-login {
        background-color: #555;
        color: #fff;
        border-radius: 0;
        width: 100%;
        height: 45px;
        border: none;
        font-weight: bold;
        margin-top: 10px;
    }
    .btn-custom-register {
        background-color: #ed4e51;
        color: #fff;
        border-radius: 0;
        width: 100%;
        height: 45px;
        border: none;
        font-weight: bold;
        margin-top: 20px;
    }

    .link-forget {
        color: red;
        text-align: center;
        display: block;
        margin-bottom: 15px;
    }

    .or-divider {
        text-align: center;
        margin-top: 20px;
        position: relative;
        color: #999;
    }
    .or-divider::before {
        content: '';
        position: absolute;
        left: 0;
        top: 50%;
        width: 45%;
        height: 1px;
        border-top: 1px dashed #ccc;
    }
    .or-divider::after {
        content: '';
        position: absolute;
        right: 0;
        top: 50%;
        width: 45%;
        height: 1px;
        border-top: 1px dashed #ccc;
    }
</style>

<div class="row" style="margin: 0;">
    <div class="col-md-7" style="padding: 0;">
        <img src="https://www.cinema.com.hk/images/bw_login_img.jpg" alt="Cinema" class="cinema-image">
    </div>

    <div class="col-md-5" style="padding: 0;">
        <div class="login-form-container">
            <h2 class="form-title">Member Login</h2>
            
            <form action="" method="post">
                {{ form.hidden_tag() }}
                
                {{ form.username(class="form-control form-control-custom", placeholder="Username / Member ID / Email") }}
                
                {{ form.password(class="form-control form-control-custom", placeholder="Password") }}
                
                <div class="checkbox" style="margin-top: -5px; font-size: 13px;">
                    <label>
                        {{ form.remember_me() }} Remember Me
                    </label>
                </div>

                <a href="{{ url_for('register') }}" class="link-forget">Forget Password?</a>

                {{ form.submit(class="btn btn-custom-login", value="Login") }}

                <div class="or-divider">Or</div>

                <a href="{{ url_for('register') }}" class="btn btn-custom-register">Register</a>
            </form>
        </div>
    </div>
</div>
{% endblock %}